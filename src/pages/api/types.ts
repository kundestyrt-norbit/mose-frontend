/**
 * Interfaces of data types fetched from server.
 */

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface Sensor {};

export interface Temperature extends Sensor {}
